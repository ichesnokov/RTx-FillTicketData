<%init>
use RTx::FillTicketData;
my $config = RTx::FillTicketData::config();
if (    $config
     && $CustomField
     && $config->{key_fields}->{ $CustomField->Id } ) {
    $m->out(   q{<td class="entry"><button class='autofill_custom_fields'>}
             . loc('Get data')
             . q{</button></td>} );
}
</%init>
<%ARGS>
$CustomField => undef
</%ARGS>
